<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>{{subject}}</title>
  <style>
    /*
     * Paleta alineada con apps/web/app/globals.css (design system).
     * Colores en hex para compatibilidad con clientes de correo.
     * --background -> #f4f6f8, --foreground -> #2d3748, --primary -> #22c55e (green),
     * --card -> #ffffff, --muted -> #f0f1f3, --muted-foreground -> #64748b, --border -> #e2e8f0
     */
    /* Reset */
    body, table, td, a { -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
    table, td { mso-table-lspace: 0pt; mso-table-rspace: 0pt; }
    img { -ms-interpolation-mode: bicubic; border: 0; height: auto; line-height: 100%; outline: none; text-decoration: none; }
    
    /* Base (--background, --foreground) */
    body {
      margin: 0 !important;
      padding: 0 !important;
      background-color: #f4f6f8;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      font-size: 16px;
      line-height: 1.6;
      color: #2d3748;
    }
    
    .email-wrapper {
      width: 100%;
      background-color: #f4f6f8;
      padding: 40px 0;
    }
    
    .email-content {
      max-width: 600px;
      margin: 0 auto;
    }
    
    /* Header */
    .email-header {
      text-align: center;
      padding: 25px 0;
    }
    
    .email-header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 700;
      color: #2d3748;
    }
    
    /* Body (--card) */
    .email-body {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
    }
    
    .email-body-inner {
      padding: 40px;
    }
    
    .content-cell {
      padding: 0;
    }
    
    h2 {
      margin: 0 0 15px;
      font-size: 20px;
      font-weight: 600;
      color: #2d3748;
    }
    
    p {
      margin: 0 0 15px;
      color: #2d3748;
    }
    
    /* Button (--primary) */
    .button {
      display: inline-block;
      padding: 14px 28px;
      background-color: #22c55e;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 14px;
      text-align: center;
    }
    
    .button:hover {
      background-color: #16a34a;
    }
    
    /* Code box (--primary para acento) */
    .code-box {
      background-color: #f0f1f3;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 24px;
      margin: 24px 0;
      text-align: center;
    }
    
    .code-value {
      font-size: 32px;
      font-weight: 700;
      letter-spacing: 8px;
      color: #22c55e;
      font-family: 'Courier New', monospace;
    }
    
    /* Amount box (--primary borde) */
    .amount-box {
      background-color: #f0fdf4;
      border-left: 4px solid #22c55e;
      padding: 16px 20px;
      margin: 20px 0;
    }
    
    .amount-label {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #64748b;
      margin-bottom: 4px;
    }
    
    .amount-value {
      font-size: 24px;
      font-weight: 700;
      color: #2d3748;
    }
    
    /* Info list (--muted) */
    .info-list {
      background-color: #f0f1f3;
      border-radius: 8px;
      padding: 16px 20px;
      margin: 20px 0;
      border: 1px solid #e2e8f0;
    }
    
    .info-list p {
      margin: 8px 0;
      font-size: 14px;
      color: #2d3748;
    }
    
    .info-list strong {
      color: #2d3748;
    }
    
    /* Warning (mantiene contraste) */
    .warning-box {
      background-color: #fffbeb;
      border: 1px solid #f59e0b;
      border-radius: 8px;
      padding: 16px;
      margin: 20px 0;
    }
    
    .warning-box p {
      margin: 0;
      font-size: 14px;
      color: #92400e;
    }
    
    /* Footer (--muted-foreground) */
    .email-footer {
      text-align: center;
      padding: 30px 40px;
    }
    
    .email-footer p {
      margin: 0 0 10px;
      font-size: 13px;
      color: #64748b;
    }
    
    .footer-links a {
      color: #64748b;
      text-decoration: underline;
      font-size: 13px;
    }
    
    @media only screen and (max-width: 620px) {
      .email-body-inner {
        padding: 24px !important;
      }
      .code-value {
        font-size: 28px !important;
        letter-spacing: 6px !important;
      }
    }
  </style>
</head>
<body>
  <table class="email-wrapper" width="100%" cellpadding="0" cellspacing="0" role="presentation">
    <tr>
      <td align="center">
        <table class="email-content" width="100%" cellpadding="0" cellspacing="0" role="presentation">
          <!-- Header -->
          <tr>
            <td class="email-header">
              <h1>Digital Wallet</h1>
            </td>
          </tr>
          
          <!-- Body -->
          <tr>
            <td class="email-body">
              <table width="100%" cellpadding="0" cellspacing="0" role="presentation">
                <tr>
                  <td class="email-body-inner">
                    <table class="content-cell" width="100%" cellpadding="0" cellspacing="0" role="presentation">
                      <tr>
                        <td>
                          {{{body}}}
                        </td>
                      </tr>
                    </table>
                  </td>
                </tr>
              </table>
            </td>
          </tr>
          
          <!-- Footer -->
          <tr>
            <td class="email-footer">
              <p>Este es un mensaje automatizado. Por favor no responda a este correo.</p>
              <p>&copy; {{year}} Digital Wallet - ePayco. Todos los derechos reservados.</p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
