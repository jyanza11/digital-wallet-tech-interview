# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database
DATABASE_URL="mysql://user:password@localhost:3306/digital_wallet"

# Email provider: resend | gmail | console (console = solo log, no envia)
EMAIL_PROVIDER=gmail

# --- Resend (EMAIL_PROVIDER=resend) ---
# En modo prueba Resend solo permite enviar al propio correo registrado en la cuenta
# RESEND_API_KEY=
# MAIL_FROM=onboarding@resend.dev

# --- Gmail (EMAIL_PROVIDER=gmail) ---
# Usar un correo Gmail y una contraseña de aplicacion
GMAIL_USER=example@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
# Opcional: remitente mostrado (por defecto se usa GMAIL_USER)
# MAIL_FROM="Digital Wallet" <correo@gmail.com>
